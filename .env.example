# Copie este arquivo para .env e ajuste os valores.
# O arquivo .env não é versionado.

# Porta HTTP do backend.
# Padrão: 3000
PORT=3000

# Caminho do arquivo do banco SQLite.
# Padrão: calcados_mariano.db na raiz do projeto.
# Os testes apontam esta variável para um arquivo temporário.
DB_PATH=./calcados_mariano.db

# Origens que o CORS aceita, separadas por vírgula.
# Deixe vazio para aceitar qualquer origem, o que serve só para desenvolvimento.
# Em produção, liste as origens do painel de estoque.
CORS_ORIGINS=http://localhost:5173

# ---- Autenticação do painel ----
# Sem estas duas variáveis, as rotas de escrita respondem 503 e o login recusa tudo.
# NÃO existe senha padrão, de propósito.
#
# Gere as duas linhas com:
#   npm run auth:hash -- "a sua senha"

# Hash scrypt da senha do dono da loja. A senha em texto nunca vai para cá.
ADMIN_SENHA_HASH=

# Segredo que assina os cookies de sessão. Trocá-lo derruba as sessões abertas.
SESSAO_SEGREDO=
