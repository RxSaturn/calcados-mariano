# Faz o npm recusar a instalação quando a versão do Node não satisfaz "engines".
# Sem isto, o npm só emite um aviso EBADENGINE e a instalação segue. Foi assim que
# um job de CI em Node 20 passou do npm ci e só quebrou dentro do teste.
engine-strict=true
